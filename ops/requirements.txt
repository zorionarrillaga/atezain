# What the deployed web service needs. The policy layer itself needs none of this: `policy/` is
# standard library only, and `make test` runs with pytest + langgraph alone.
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.20
openpyxl==3.1.5
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
psycopg[binary]==3.3.5          # the Postgres store (PLAN.md §4.1)
# added when the graph's checkpointer moves to Postgres too (PLAN.md §4.2), not before:
# langgraph-checkpoint-postgres==*
